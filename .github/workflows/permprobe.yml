name: permprobe

# TEMPORARY. The caller grants exactly what all 35 estate callers grant.
on:
  workflow_dispatch:

jobs:
  capped:
    uses: ./.github/workflows/permprobe-called.yml
    with:
      label: caller granted packages read
    permissions:
      contents: read
      packages: read
